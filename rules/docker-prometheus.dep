
DPATH       := $($(DOCKER_PROMETHEUS)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-prometheus.mk rules/docker-prometheus.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_PROMETHEUS)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_PROMETHEUS)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_PROMETHEUS)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_PROMETHEUS),$(DOCKER_DHCP_RELAY_DBG)))
